# claude-dejavu
Hook PreToolUse intercetta tool deterministici (Bash/Read/Grep/Glob), calcola hash(cmd+cwd+mtime) e serve cache. PostToolUse salva risultati. Stack Node/TS + SQLite/JSON. Risultato: -20-40% token in sessioni lunghe, latenza ridotta. Zero competitor espliciti. Demo: video 1h prima/dopo + grafico token.
