description = "Show claude-dejavu cache hit rate, byte usage and per-tool breakdown."
prompt = "Run the Bash tool with this exact command and print the output verbatim, nothing else: node \"${CLAUDE_PLUGIN_ROOT}/scripts/cli.js\" stats"
