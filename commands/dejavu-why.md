---
description: Inspect a single claude-dejavu cache entry by its hash (full or prefix from a CACHE_HIT marker).
argument-hint: "<hash>"
---

Run the Bash tool with this exact command and print the output verbatim, without summarising or interpreting it:

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/cli.js" why $ARGUMENTS
```
