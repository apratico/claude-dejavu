description = "Wipe the claude-dejavu cache. Pass --tool <ToolName> to scope the wipe to one tool."
prompt = "Run the Bash tool with this exact command and print the output verbatim, nothing else: node \"${CLAUDE_PLUGIN_ROOT}/scripts/cli.js\" clear {{args}}"
